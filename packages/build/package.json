{
  "name": "@packlet/build",
  "version": "0.2.2",
  "description": "Packlet build wrapper: build ESM, d.ts, and CJS (opt-in) for packages and expose programmatic API.",
  "license": "MIT",
  "homepage": "https://github.com/kazvizian/packlet-js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kazvizian/packlet-js.git",
    "directory": "packages/build"
  },
  "bugs": {
    "url": "https://github.com/kazvizian/packlet-js/issues"
  },
  "author": {
    "name": "KazViz",
    "email": "kazviz@nazator.com",
    "url": "https://kazviz.com"
  },
  "main": "dist/index.mjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs"
    }
  },
  "bin": {
    "packlet-build": "dist/cli.mjs"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.mjs --format=esm --target=node --minify && bun build src/cli.ts --outfile=dist/cli.mjs --format=esm --target=node --minify && bun x tsc -p tsconfig.json --emitDeclarationOnly --outDir dist && (test -f dist/.tsbuildinfo && mv dist/.tsbuildinfo .tsbuildinfo || true) && chmod +x dist/cli.mjs",
    "clean": "rm -rf dist .tsbuildinfo",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {},
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "packlet": {
    "gpr": true,
    "gprName": "packlet-build"
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=18"
  },
  "packageManager": "bun@1.3.1"
}
